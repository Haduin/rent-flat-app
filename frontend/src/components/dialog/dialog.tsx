import React from 'react';

interface DialogProps {
    visible: boolean;
    onHide?: () => void;
    onSave?: () => void;
    onCancel?: () => void;
}

const Dialog = () => {
    return (
        <div>

        </div>
    );
};

export default Dialog;
